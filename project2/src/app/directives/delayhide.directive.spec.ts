import { DelayhideDirective } from './delayhide.directive';

describe('DelayhideDirective', () => {
  it('should create an instance', () => {
    const directive = new DelayhideDirective();
    expect(directive).toBeTruthy();
  });
});
